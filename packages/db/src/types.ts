// This file is regenerated from the live Supabase schema via:
//   pnpm db:types
// (which runs `supabase gen types typescript --project-id <ref> --schema public`)
//
// Do NOT hand-edit. Run the script after every migration.

// Placeholder — will be populated by `pnpm db:types` after migrations are applied.
// The real file is ~1500 lines of auto-generated Supabase types.

export interface Database {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
  };
}
